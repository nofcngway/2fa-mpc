---
phase: 04-shamir-secret-sharing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - twofa/internal/crypto/shamir/gf256.go
  - twofa/internal/crypto/shamir/gf256_test.go
autonomous: true
requirements:
  - CRYPTO-02
must_haves:
  truths:
    - "gfAdd(a, b) == a XOR b for all 256x256 input pairs"
    - "gfMul(a, 1) == a for all 256 elements (multiplicative identity)"
    - "gfMul(a, gfInv(a)) == 1 for all 255 non-zero elements"
    - "gfMul(a, gfAdd(b, c)) == gfAdd(gfMul(a, b), gfMul(a, c)) for all 16M triples (distributivity)"
    - "gfMul(a, 0) == 0 and gfMul(0, b) == 0 for all elements"
    - "Log/exp tables are consistent: expTable[logTable[x]] == x for all x in 1..255"
  artifacts:
    - path: "twofa/internal/crypto/shamir/gf256.go"
      provides: "GF(256) finite field arithmetic with log/exp tables"
      contains: "func init()"
    - path: "twofa/internal/crypto/shamir/gf256_test.go"
      provides: "Exhaustive property tests for GF(256) field axioms"
      contains: "TestGF256"
  key_links:
    - from: "twofa/internal/crypto/shamir/gf256.go"
      to: "twofa/internal/crypto/shamir/gf256.go"
      via: "init() populates logTable and expTable used by gfMul, gfDiv, gfInv"
      pattern: "logTable|expTable"
---

<objective>
Implement GF(256) finite field arithmetic as the mathematical foundation for Shamir Secret Sharing.

Purpose: All Shamir operations (polynomial evaluation, Lagrange interpolation) depend on correct GF(256) arithmetic. This must be proven correct before building Split/Combine on top.
Output: `gf256.go` with log/exp tables and arithmetic functions, `gf256_test.go` with exhaustive property tests proving field axiom correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shamir-secret-sharing/04-CONTEXT.md
@.planning/phases/04-shamir-secret-sharing/04-RESEARCH.md
@workspace/03 - Security/Shamir Secret Sharing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GF(256) arithmetic — tests first, then implementation</name>
  <files>twofa/internal/crypto/shamir/gf256.go, twofa/internal/crypto/shamir/gf256_test.go</files>
  <read_first>
    - twofa/go.mod (verify module path and Go version)
    - .planning/phases/04-shamir-secret-sharing/04-RESEARCH.md (GF(256) code examples, pitfalls)
    - workspace/03 - Security/Shamir Secret Sharing.md (algorithm spec)
  </read_first>
  <behavior>
    GF(256) field axiom tests (all in gf256_test.go, package shamir):

    1. TestGF256_ExpLogTableConsistency: expTable[logTable[x]] == x for all x in 1..255
    2. TestGF256_AddCommutativity: gfAdd(a, b) == gfAdd(b, a) for all 256x256 pairs
    3. TestGF256_AddAssociativity: gfAdd(gfAdd(a, b), c) == gfAdd(a, gfAdd(b, c)) for sampled triples
    4. TestGF256_AddIdentity: gfAdd(a, 0) == a for all 256 elements
    5. TestGF256_AddInverse: gfAdd(a, a) == 0 for all 256 elements (self-inverse in GF(2^n))
    6. TestGF256_MulCommutative: gfMul(a, b) == gfMul(b, a) for all 256x256 pairs
    7. TestGF256_MulAssociative: gfMul(gfMul(a, b), c) == gfMul(a, gfMul(b, c)) for sampled triples
    8. TestGF256_MulIdentity: gfMul(a, 1) == a for all 256 elements
    9. TestGF256_MulZero: gfMul(a, 0) == 0 and gfMul(0, b) == 0 for all 256 elements
    10. TestGF256_MulInverse: gfMul(a, gfInv(a)) == 1 for all 255 non-zero elements
    11. TestGF256_Distributive: gfMul(a, gfAdd(b, c)) == gfAdd(gfMul(a, b), gfMul(a, c)) for all 16M+ triples
    12. TestGF256_DivInverse: gfDiv(gfMul(a, b), b) == a for all non-zero b, all a
    13. TestGF256_DivByZeroPanics: gfDiv(x, 0) panics for any x
    14. TestGF256_InvZeroPanics: gfInv(0) panics
  </behavior>
  <action>
    **RED phase — write tests first:**

    Create directory `twofa/internal/crypto/shamir/` (mkdir -p).

    Create `twofa/internal/crypto/shamir/gf256_test.go` with package declaration `package shamir` and all 14 tests listed in behavior section. Use standard `testing` package only. For exhaustive tests (256x256 and 256x256x256), iterate all values — GF(256) is small enough. For panic tests, use `defer recover()` pattern. Run tests — they MUST fail (functions don't exist yet).

    **GREEN phase — implement gf256.go:**

    Create `twofa/internal/crypto/shamir/gf256.go` with package declaration `package shamir`:

    1. Declare package-level variables:
       ```go
       var logTable [256]byte
       var expTable [256]byte
       ```

    2. Implement `gfMulNoTable(a, b byte) byte` — shift-and-XOR (Russian peasant multiplication) with reduction by 0x1B (lower 8 bits of irreducible polynomial 0x11B per D-07):
       - Loop while b > 0
       - If b&1 != 0: result ^= a
       - Save carry = a & 0x80, shift a <<= 1
       - If carry != 0: a ^= 0x1B
       - Shift b >>= 1

    3. Implement `func init()` (per D-08) to generate log/exp tables at runtime:
       - Start with x = byte(1), generator element = 3
       - Loop i from 0 to 254: expTable[i] = x, logTable[x] = byte(i), x = gfMulNoTable(x, 3)
       - Set expTable[255] = expTable[0] (wrap)
       - logTable[0] stays 0 (undefined, never used correctly)

    4. Implement arithmetic functions (per D-09), all unexported:
       - `gfAdd(a, b byte) byte` — return a ^ b
       - `gfMul(a, b byte) byte` — guard: if a == 0 || b == 0 return 0; then return expTable[(int(logTable[a])+int(logTable[b]))%255]
       - `gfDiv(a, b byte) byte` — guard: if b == 0 panic("shamir: division by zero in GF(256)"); if a == 0 return 0; then return expTable[(int(logTable[a])-int(logTable[b])+255)%255]
       - `gfInv(a byte) byte` — guard: if a == 0 panic("shamir: inverse of zero in GF(256)"); return expTable[255-int(logTable[a])]

    Run all tests — they MUST pass.

    **REFACTOR phase:** Review for clarity, add doc comments to each function explaining the mathematical operation. Ensure no `math/rand` import (only `crypto/rand` is allowed in this package, though not used in gf256.go).
  </action>
  <verify>
    <automated>cd /Users/vbncursed/programming/2fa/twofa && go test ./internal/crypto/shamir/ -run TestGF256 -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `twofa/internal/crypto/shamir/gf256.go` exists with `package shamir` declaration
    - `gf256.go` contains `func init()` that populates logTable and expTable
    - `gf256.go` contains `func gfAdd(a, b byte) byte`
    - `gf256.go` contains `func gfMul(a, b byte) byte`
    - `gf256.go` contains `func gfDiv(a, b byte) byte`
    - `gf256.go` contains `func gfInv(a byte) byte`
    - `gf256.go` contains `0x1B` (reduction polynomial)
    - `gf256.go` does NOT contain `import "math/rand"`
    - File `twofa/internal/crypto/shamir/gf256_test.go` exists with `package shamir`
    - `gf256_test.go` contains at least 14 test functions matching `TestGF256_`
    - `go test ./internal/crypto/shamir/ -run TestGF256 -v -count=1` exits 0 with all tests PASS
  </acceptance_criteria>
  <done>All 14 GF(256) property tests pass, proving the finite field arithmetic is mathematically correct across all 256 elements</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package API | gf256 functions are unexported; only consumed by shamir.go within same package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | gfMulNoTable timing | accept | Table lookups after init are constant-time; gfMulNoTable only runs during init(), not on secret data |
| T-04-02 | Tampering | logTable/expTable | accept | Package-level vars, only written once in init(); Go init() runs before any other code; no exported mutation path |
</threat_model>

<verification>
- `cd /Users/vbncursed/programming/2fa/twofa && go test ./internal/crypto/shamir/ -run TestGF256 -v -count=1` — all 14 tests pass
- `go vet ./internal/crypto/shamir/` — no issues
</verification>

<success_criteria>
GF(256) arithmetic is proven correct: addition (XOR), multiplication (log/exp tables), division, and inverse all satisfy field axioms across exhaustive input ranges. Log/exp tables are generated at runtime in init() using generator 3 and polynomial 0x11B.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shamir-secret-sharing/04-01-SUMMARY.md`
</output>
